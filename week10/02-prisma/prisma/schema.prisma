// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://polakisaikiran1:Da4ZxwbvklM0@ep-square-leaf-a5tbp99z.us-east-2.aws.neon.tech/TestAmatarasu?sslmode=require"
}

model User {
  id Int    @id @default(autoincrement())
  email String @unique
  firstName String?
  lastName String?
  password String
}

model Notes{
  id Int @id @default(autoincrement())
  title String
  done Boolean @default(false)
  description String?
  userId Int
}

// * Change the Schema then run two commands
// * run migration ~npx prisma migrate dev --name Initialize the schema
// * run autogenerated clients ~npx prisma generate