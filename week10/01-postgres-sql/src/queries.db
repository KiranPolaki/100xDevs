CREATE TABLE customer(
	first_name VARCHAR(30) NOT NULL,
	last_name VARCHAR(30) NOT NULL,
	email VARCHAR(60) NOT NULL,
	company VARCHAR(60) NOT NULL,
	street VARCHAR(50) NOT NULL,
	city VARCHAR(40) NOT NULL,
	state CHAR(2) NOT NULL,
	zip SMALLINT NOT NULL,
	phone VARCHAR(20) NOT NULL,
	birth_date DATE NULL,
	sex CHAR(1) NOT NULL,
	date_entered TIMESTAMP NOT NULL,
	id SERIAL PRIMARY KEY
);

INSERT INTO customer(first_name,last_name,email,company,street,city,state,zip,phone,birth_date,sex,date_entered)
VALUES ('sai','kiran','sai@gmail.com','mcdonalds','5-291 hyderabad','hyderabad','TS','30084','990511900','2002-08-26','M', current_timestamp)

-- * Creating types
CREATE TYPE sex_type as enum
('M','F')

-- * Alter table
ALTER TABLE customer
ALTER COLUMN sex TYPE sex_type USING sex::sex_type	